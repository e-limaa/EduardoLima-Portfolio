import { SectionHeader } from '@antigravity/ds';

<DocsPageHeader 
  title="SectionHeader" 
  description="A consistent header block for page sections, layout out title, description, and an optional index/label." 
  status="draft" 
/>

<ComponentPlayground
  controls={[
    { name: 'title', type: 'text', defaultValue: 'My Section' },
    { name: 'description', type: 'text', defaultValue: 'A brief description of this section.' },
    { name: 'index', type: 'text', defaultValue: '01' },
    { name: 'label', type: 'text', defaultValue: 'LABEL' },
    { name: 'align', type: 'select', options: ['left', 'center'], defaultValue: 'left' }
  ]}
  codeTemplate={(props) => `
import { SectionHeader } from '@antigravity/ds';

export default function Example() {
  return (
    <SectionHeader 
      title="${props.title}" 
      description="${props.description}"
      index="${props.index}"
      label="${props.label}"
      align="${props.align}"
    />
  );
}
  `}
>
  {(props) => (
    <div className="p-8 border rounded-lg bg-background">
        <SectionHeader {...props} />
    </div>
  )}
</ComponentPlayground>

## Usage

Use this component at the top of every major section (e.g., Projects, Services, About) to maintain visual consistency.

### Center Aligned

<ComponentPreview>
  <SectionHeader 
      title="Centered Title" 
      description="This is center aligned text for sections that need a different layout."
      align="center"
  />
</ComponentPreview>
